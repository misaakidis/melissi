[package]
name = "melissi-types"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "the model's identity seam, single-sourced: Triple, PeerId, Bin, BinId"

[dependencies]
