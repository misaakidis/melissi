[package]
name = "melissi-protobuf"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "the proto3 wire plumbing bee speaks on every protocol — varint, gogo delimited framing, field codec. Hand-rolled, zero deps, single-sourced for wire + net."

[dependencies]
