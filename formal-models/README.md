# Formal models

The specs of record that the crates refine. One subdirectory per tool; `tla/`
(TLA+, model-checked with TLC) is the first. The `cargo test` parity matrix
re-checks the same suite in Rust, so the default build needs no external tool.

Licensed under the repository's BSD-3-Clause `LICENSE`.
